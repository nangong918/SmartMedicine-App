关系: 订单业务在

预约:
MedicineConstant.APPOINTMENT
↓
分布式锁进行行为幂等性校验: (防止用户重复预约; 相比于检查标记位或者信息量, 锁更直接)
dataId: merchantId:userId
mappingPath: MedicineConstant.RegisterAppointment_CONTROLLER + MedicineConstant.APPOINTMENT
↓
生成订单id并缓存到redis: 用于在审核结果出来之间交给用户进行查询视图 (在事务审核期间如果用户主动查询订单状态, 那么需要将数据从Redis中取出)
↓
异步生成订单进行订单审核, 操作丢入mq降低并发操作 (降低qps)
↓
创建待支付定时mq消息 (需要在过期之后进行主动触发; 所以不能单纯的标记为Redis记录并设置过期时间)

待支付:
medicine订单系统创建待支付订单放入延迟队列
↓
延时队列的消息触发
* 已处理(无)
* 超时未处理: 执行超时操作
  * 归还数据库库存     (问题: 如果在支付之前检查订单未过期, 然后执行支付事务, 但是事务执行过程中消息延迟队列触发了, 然后将库存归还了商品数量, 此时久造成了超卖事故)
  * 解除申请分布式锁
  * netty通知前端


支付: (支付成功之后应该mq通知订单系统取改变订单状态, 而不是在支付系统中执行, 这些应该都是异步操作.)
PurchaseConstant.AppointmentPay_API
↓
分布式锁进行行为幂等性校验:
dataId: userId:orderId
mappingPath: PurchaseConstant.Pay_CONTROLLER + PurchaseConstant.AppointmentPay_API
↓
事务支付订单 (支付成功之后包括: Redis标注已支付订单)
↓
支付结果通知medicine-service:
* 订单状态更换
* 解除支付分布式锁
* netty通知前端

取消订单: 